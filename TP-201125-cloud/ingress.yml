apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
spec:
  rules:
  - host: kube.thfx.fr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: http
            port:
              name: http
